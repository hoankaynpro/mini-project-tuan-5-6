# Tuần 5-6: Nghiên cứu Webpack, classnames, SASS và Reactstrap

## 🧩 1. Webpack
Dùng để bundle các file TS/JS, CSS, SCSS thành mã chạy được trên trình duyệt.

## 📦 2. classnames
Hỗ trợ gộp class CSS linh hoạt, ví dụ:
```tsx
classNames("btn", { "btn-active": isActive });
```

## 🎨 3. SASS
CSS mở rộng có biến `$variable`, nesting, mixin...  
Code trong `SassDemo.scss`

## ⚛️ 4. Reactstrap
Thư viện giao diện dựa trên Bootstrap.  
Code trong `ReactstrapDemo.tsx`

## 🚀 Cách chạy
```bash
npm install
npm start
```
Ứng dụng sẽ mở tại: **http://localhost:3000**
